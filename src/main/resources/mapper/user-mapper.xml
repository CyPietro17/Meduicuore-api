<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="it.pietro.salvatore.medicuore.mappers.UserMapper">
    <resultMap id="userMap" type="user">
        <id property="id" javaType="Long" column="id"/>
        <result property="username" javaType="String" column="username"/>
        <result property="email" javaType="String" column="email"/>
        <result property="password" javaType="String" column="password"/>
        <result property="role" javaType="it.pietro.salvatore.medicuore.entity.Role" column="role"/>
    </resultMap>

    <select id="findByUsername" resultMap="userMap">
        SELECT *
        FROM public."my_user"
        WHERE username = #{username};
    </select>

    <select id="findAll" resultMap="userMap">
        SELECT *
        FROM public."my_user";
    </select>

    <insert id="save" parameterType="user" keyProperty="id" keyColumn="id">
        INSERT INTO public."my_user"
            ("username", "password", "email", "role")
        VALUES (#{username}, #{password}, #{email}, #{role});
    </insert>
</mapper>